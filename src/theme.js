/* ============================================================
   PALETTE — JS source of truth for colors used in Framer Motion.
   Mirror any changes in index.css :root block.
   ============================================================ */
export const colors = {
  black: '#080808',
  white: '#f0ede6',
  red:   '#ff3c00',
  gray:  '#1a1a1a',
  mid:   '#888',
}
